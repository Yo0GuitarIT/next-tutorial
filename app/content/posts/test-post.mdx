---
title: '測試文章'
date: '2025-08-20'
---

## 這是一篇測試文章

歡迎來到我的新部落格！這篇文章是透過 Gemini 自動建立的，用來驗證 MDX 渲染功能。

### 功能列表

- [x] 文章列表頁面
- [x] 動態文章內容頁面
- [ ] 更多功能即將推出！

這是一個段落，包含 **粗體** 和 *斜體* 文字。

```tsx
import React from 'react';

const MyComponent = () => {
  return <div>Hello, MDX!</div>;
};

export default MyComponent;
```

現在，讓我們看看真正的 React 元件渲染結果：

<HelloMdx />
